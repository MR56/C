#include <stdio.h>

int main(){

    char primero, ultimo; 
    printf ("Intorduczca su primera y ultima parte inicial:: ");
    scanf(" %c %c", &primero, &ultimo);
    printf("Hola, %c, %c \n", primero, ultimo);  

return 0;  
}
